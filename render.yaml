services:
  - type: web
    name: atlas2-0
    env: python
    buildCommand: |
      cd /opt/render/project
      npm install
      npm run build
      pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: PORT
        value: 10000
    rootDir: /