{
  "name": "atlas2.0",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && echo 'Build complete!'",
    "start": "python server.py",
    "dev": "concurrently \"cd frontend && npm start\" \"python server.py\"",
    "clean": "rm -rf build && rm -rf frontend/build && rm -rf frontend/node_modules/.cache"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}